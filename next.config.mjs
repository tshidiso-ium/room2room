/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'cdn.yoursite.com'],
        unoptimized: true 
      },
};

export default nextConfig;
